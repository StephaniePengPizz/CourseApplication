<resources>
    <!-- Base application theme -->
    <style name="Theme.MyTimetable" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme with colors, fonts, and other elements -->

        <!-- Primary color -->
        <item name="colorPrimary">@color/colorPrimary</item>

        <!-- Darker primary color -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- Accent color -->
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Window background (app background color) -->
        <item name="android:windowBackground">@android:color/white</item>

        <!-- Customize ActionBar / Toolbar -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>

        <!-- Customizable UI elements (e.g., Button, TextView, etc.) -->
        <!-- You can add more custom styles here -->
    </style>
</resources>